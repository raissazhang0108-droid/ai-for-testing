# prompt_templates.py

REQUIREMENT_REVIEW_PROMPT = """
你是一位资深的高级测试专家、产品质量合规审计师。
请严格基于 [IEEE 29148] 需求工程规范，对以下输入的原始需求（PRD/原型提取文本）进行深度逆向审计与全要素提取。

【输入原始需求】:
{raw_requirement_text}

【硬核任务要求】:
1. 结构化抽取：梳理出 7 要素结构（主体、动作、前置条件、后置状态、数据流、异常流、隐式非功能性要求）。
2. 逻辑漏洞识别：找出需求中含糊不清、未定义异常、边界冲突或可能导致线上资损的逻辑盲区。
3. 测试骨架编排：规划出主基线链路、核心边界值矩阵、主备路径划分。

【输出格式限制】:
请严格输出为 JSON 格式，不得包含任何 Markdown 包裹标记（如 ```json），直接以 {{ 交付：
"""
